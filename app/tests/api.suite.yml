actor: ApiTester
modules:
  enabled:
    - REST:
        url: /
        depends: Symfony
        part: Json
    - Symfony:
        app_path: 'src'
        environment: 'test'
    - Doctrine2:
        depends: Symfony
        cleanup: true
    - Asserts
    - \App\Tests\Helper\Api
